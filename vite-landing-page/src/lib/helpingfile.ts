import {FontAwesomeIcon} from '@fortawesome/react-fontawesome';
import Particles from "@/components/ui/particles"; // Particles from Magic UI
import { Dock, DockIcon, dockVariants } from "@/components/ui/dock"; // Particles from Magic UI

export {FontAwesomeIcon, Particles, Dock, DockIcon, dockVariants};
